- name: conditions
  hosts: localhost
  tasks:
  - name: check user exist or not
    ansible.builtin.command: id expense
    ignore_errors: true

  - name: say hello
    ansible.builtin.debug:
      msg: "hello"
